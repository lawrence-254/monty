#include "monty.h"
/**
 * f_push -  pushes an element to the stack
 * @head: head of stack
 * @count: tracks size of stack
 * Return: void
 */
void f_push(stack_t **head, unsigned int counter)
{
	int i, j = 0;
